// 차단 목록 페이지 컴포넌트: 차단한 사용자 목록 관리 기능 (개발 예정)
// TODO: 차단 목록 기능 구현 예정 - 현재는 기본 레이아웃만 표시

/**
 * 차단 목록 페이지 컴포넌트
 * 
 * 기능 (구현 예정):
 * - 차단한 사용자 목록 조회
 * - 차단 해제 기능
 * - 사용자 프로필 정보 표시
 * - 차단 일시 및 사유 표시
 * - 차단된 사용자와의 상호작용 제한
 * 
 * 현재 상태:
 * - 페이지 기본 구조만 구현됨
 * - 향후 차단 목록 기능 추가 예정
 * - 임시 안내 메시지 표시
 */
export default function BlockListPage() {
  return (
    <div className="p-4">
      {/* 페이지 제목 */}
      <h1 className="text-xl font-bold mb-4">차단 목록</h1>
      
      {/* 개발 안내 메시지 */}
      <p className="text-gray-400 text-sm">구현 예정</p>
    </div>
  )
}
