// PostCSS 설정 파일: CSS 전처리기 플러그인 설정
// Tailwind CSS와 Autoprefixer를 사용하여 CSS를 최적화합니다.

export default {
  plugins: {
    // Tailwind CSS: 유틸리티 클래스를 실제 CSS로 변환
    tailwindcss: {},
    
    // Autoprefixer: 브라우저 호환성을 위한 벤더 프리픽스 자동 추가
    autoprefixer: {},
  },
}
